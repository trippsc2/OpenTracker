namespace OpenTracker.Models.AutoTracking.SNESConnectors
{
    /// <summary>
    /// This enum type defines the address translation mode values.
    /// </summary>
    public enum TranslationMode
    {
        Read,
        Write
    }
}